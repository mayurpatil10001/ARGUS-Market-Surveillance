# requirements-prod.txt — Production API dependencies only
# Dev-only packages (streamlit, plotly, pytest, kiteconnect, pyvis, dowhy) are excluded.
# torch + torch-geometric are installed separately in Dockerfile.api
# (CPU-only build from https://download.pytorch.org/whl/cpu)

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
uvloop>=0.19.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# ── Cache / queue (optional — backend gracefully handles missing) ───────────────
redis==5.0.4
kafka-python==2.0.2

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ── ML / AI ───────────────────────────────────────────────────────────────────
# torch + torch-geometric installed separately (CPU wheel) — see Dockerfile.api
scikit-learn==1.4.2
pyod==1.1.3
numpy==1.26.4
scipy==1.13.0
pandas==2.2.2
transformers>=4.40.0
sentencepiece>=0.2.0

# ── Document parsing ──────────────────────────────────────────────────────────
pdfplumber==0.11.0
python-docx==1.1.2

# ── Network / scraping ────────────────────────────────────────────────────────
requests==2.31.0
beautifulsoup4==4.12.3
networkx==3.3

# ── API utilities ─────────────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
sse-starlette==2.1.0
msgpack==1.0.8
httpx==0.27.0
reportlab==4.2.0
